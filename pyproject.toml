[project]
name = "postauth"
version = "0.1.0"
description = "API de productos con autenticación"
readme = "README.md"
requires-python = ">=3.12, <3.14"
dependencies = [
    "django>=4.2.30",
    "django-bootstrap-v5>=1.0.11",
    "python-dotenv>=1.2.2",
]
